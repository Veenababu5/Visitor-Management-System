// ─── Theme ────────────────────────────────────────────────────────────────────
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary navy palette ──────────────────────────────────────────────────
  static const Color navyPrimary  = Color(0xFF0D2137);
  static const Color navyDark     = Color(0xFF091929);
  static const Color navyMedium   = Color(0xFF1A3A5C);
  static const Color navyLight    = Color(0xFF2C4F6E);

  // ── Blue accent ───────────────────────────────────────────────────────────
  static const Color blueAccent   = Color(0xFF1565C0);
  static const Color blueMedium   = Color(0xFF1976D2);
  static const Color blueLight    = Color(0xFFE3F2FD);

  // ── Neutral ───────────────────────────────────────────────────────────────
  static const Color white        = Color(0xFFFFFFFF);
  static const Color background   = Color(0xFFF5F7FA);
  static const Color surfaceGrey  = Color(0xFFF8FAFC);
  static const Color border       = Color(0xFFE2E8F0);
  static const Color divider      = Color(0xFFF1F5F9);

  // ── Text ──────────────────────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textLight     = Color(0xFF94A3B8);
  static const Color textHint      = Color(0xFFCBD5E1);
  static const Color textWhite     = Color(0xFFFFFFFF);

  // ── Approved / Green ──────────────────────────────────────────────────────
  static const Color approved       = Color(0xFF16A34A);
  static const Color approvedDark   = Color(0xFF15803D);
  static const Color approvedLight  = Color(0xFFDCFCE7);
  static const Color approvedBorder = Color(0xFF86EFAC);

  // ── Pending / Amber ───────────────────────────────────────────────────────
  static const Color pending        = Color(0xFFD97706);
  static const Color pendingDark    = Color(0xFFB45309);
  static const Color pendingLight   = Color(0xFFFEF3C7);
  static const Color pendingBorder  = Color(0xFFFCD34D);

  // ── Rejected / Red ────────────────────────────────────────────────────────
  static const Color rejected        = Color(0xFFDC2626);
  static const Color rejectedDark    = Color(0xFFB91C1C);
  static const Color rejectedLight   = Color(0xFFFEE2E2);
  static const Color rejectedBorder  = Color(0xFFFCA5A5);

  // ── Completed / Blue ──────────────────────────────────────────────────────
  static const Color completed      = Color(0xFF2563EB);
  static const Color completedLight = Color(0xFFEFF6FF);

  // ── Input ─────────────────────────────────────────────────────────────────
  static const Color inputBorder  = Color(0xFFE2E8F0);
  static const Color inputFocused = Color(0xFF1565C0);
  static const Color inputBg      = Color(0xFFFAFBFC);
}
